{
  "name": "watch-to-run",
  "version": "1.0.5",
  "description": "A simple CLI that watches the given file for changes and executes it.",
  "homepage": "https://github.com/divyanshyadav/watch-to-run",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish:patch": "npm version patch && git push && git push --tags && npm publish"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.5.1",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2"
  },
  "bin": {
    "watchtorun": "./cli.js"
  },
  "dependencies": {},
  "keywords": [
    "watch",
    "node",
    "cli",
    "watcher"
  ]
}
